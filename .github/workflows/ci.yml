name: The Stress Engine CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  audit-and-test:
    uses: Fort-Knox/cicd-standards/.github/workflows/node-standard.yml@main
    with:
      node-version: '20'
      test-command: 'npm test'
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
